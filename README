This is a very simple example of how to do texturing with OpenGL 4.5 while still
using glBegin...glEnd and skipping the whole shader language.   This first
example relies on SOIL for loading images but is very inefficient because everything
is included in the display function.

